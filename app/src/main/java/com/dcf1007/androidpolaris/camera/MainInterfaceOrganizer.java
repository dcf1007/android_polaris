package com.dcf1007.androidpolaris.camera;

/**
 * Obsolete UI organizer placeholder.
 *
 * <p>The main activity now builds the final UI directly. This class intentionally has no runtime
 * behavior and remains only to avoid stale build artifacts during incremental source replacement.</p>
 */
final class MainInterfaceOrganizer {
    private MainInterfaceOrganizer() { }
}
